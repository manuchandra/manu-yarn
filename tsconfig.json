stage('Typescript build') {
    steps {
        // Ensure all dependencies are installed
        sh 'yarn install --immutable'
        
        // Generate a tsconfig.json file if one doesn't exist
        sh 'tsc --init'
        
        // Run the build command
        sh 'yarn build'
    }
}
